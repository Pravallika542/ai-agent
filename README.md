# AI Agent using Google Gemini

This project demonstrates a simple AI agent built using the Google Gemini API.

The agent takes a text prompt as input and generates a response using the Gemini model.

---

## ⚠️ API Key Notice

For security reasons, **no real API key is included in this repository**.

In the source code (`tools.py`), you will see a placeholder text instead of an actual API key.

```python
api_key="paste your google api key here"
